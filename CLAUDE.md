# CLAUDE.md — 日本株 自動売買システム

このファイルは Claude Code がこのリポジトリで作業する際のガイドであり、
プロジェクトの要件定義書を兼ねます。設計判断や前提が変わったら本書を更新してください。

---

## 1. プロジェクト概要

日本株を対象とした**自動売買システム**を構築する。
機械学習を視野に入れた戦略を、**バックテスト → ペーパートレード → 本番自動発注**の
順で段階的に実用化していく。安全性を最優先とし、いきなり実弾を投入しない。

### 開発フェーズ（段階的ゴール）

| フェーズ | 内容 | 実弾リスク | 状態 |
|---|---|---|---|
| **Phase 1** | バックテスト基盤（データ取得・特徴量・戦略・約定シミュレーション・評価） | なし | **最優先・着手中** |
| Phase 2 | ペーパートレード（リアルタイムデータで仮想発注、挙動検証） | なし | 未着手 |
| Phase 3 | 本番自動発注（少額から、リスク管理を厳重に） | あり | 未着手 |

**現時点のスコープは Phase 1（バックテスト基盤）。** Phase 2/3 を見据えて
インターフェースを抽象化しておくが、実発注コードは Phase 1 では書かない。

---

## 2. 確定した要件（ヒアリング結果）

| 項目 | 決定 | 補足 |
|---|---|---|
| 言語 / 実行環境 | **Python** | データ分析・ML・バックテスト資産が豊富 |
| 最初のゴール | **バックテスト基盤から** | 実弾リスクなしで戦略検証 |
| 戦略 | **機械学習を視野（未確定）** | 戦略は差し替え可能な設計に。まずルールベースのベースラインも用意 |
| 証券会社 / 発注API | **未定（相談）** | Broker 層を抽象化し後から決定可能に |
| 株価データソース | **未定（推奨を採用）** | → **J-Quants API を推奨**（後述） |
| 対象ユニバース | **おまかせ** | → **東証プライム中心**から開始（後述） |
| リスク管理 | 損切り/利確・ポジションサイズ・最大DD制限・取引コスト/スリッページ | **全て必須要件** |

### 2.1 推奨デフォルト（未定項目に対する提案）

未定だった項目は、以下を初期デフォルトとして進める。変更したい場合は本書を更新。

- **データソース: J-Quants API**
  JPX（日本取引所グループ）公式のヒストリカルデータ。日足・財務・信用取引・
  決算発表予定などが取得でき、日本株 ML に最も適する。無料プランあり。
  まずは無料プランで取得できる範囲（過去2年程度の日足）から始める。
  - 補助として `yfinance` でのプロトタイピングも可能にしておく（精度・網羅性は劣る）。
- **ユニバース: 東証プライム上場の流動性上位**
  まず流動性の高い主要銘柄（例: 売買代金上位 ~500 銘柄）に絞る。
  流動性フィルタにより ML のノイズとスリッページを抑える。
  銘柄リストは設定ファイルで管理し、後から全上場銘柄へ拡張可能にする。
- **発注 Broker（Phase 3 想定）: au カブコム証券 kabu ステーション API を第一候補**
  個人向けで現物・信用の発注、板情報取得が可能な数少ない国内 API。
  Interactive Brokers（グローバル展開向け）も Broker 抽象化で差し替え可能にする。

---

## 3. 必須リスク管理要件

バックテスト・本番いずれにおいても、以下を**戦略から独立した共通モジュール**として実装する。
戦略ロジックがバグっても、リスク層が最終防衛線として機能すること。

1. **損切り / 利確（ストップ）**
   ポジションごとに stop-loss / take-profit を設定。トレーリングストップも検討。
2. **ポジションサイズ管理**
   1 銘柄あたりの最大エクスポージャ、ポートフォリオ全体の最大エクスポージャを制限。
   固定比率・ボラティリティ（ATR）ベースのサイジングをサポート。
3. **最大ドローダウン制限**
   一定のドローダウンに達したら新規エントリーを停止するサーキットブレーカー。
4. **取引コスト・スリッページのモデル化**
   手数料（証券会社の体系）とスリッページをバックテストに必ず織り込む。
   楽観的なバックテスト結果を防ぐため、保守的なコスト前提を既定とする。

---

## 4. アーキテクチャ方針

戦略・データソース・発注先を**いつでも差し替えられる**よう、各層をインターフェースで分離する。

```
┌────────────────────────────────────────────────────────────┐
│  Strategy 層（差し替え可能：ルールベース / ML）              │
│  - シグナル生成のみ担当。発注やリスク判定は持たない           │
└───────────────┬────────────────────────────────────────────┘
                │ signals
┌───────────────▼────────────────────────────────────────────┐
│  Risk / Portfolio 層（共通・必須）                           │
│  - ポジションサイズ、損切り利確、最大DD、コスト適用           │
└───────────────┬────────────────────────────────────────────┘
                │ orders
┌───────────────▼────────────────────────────────────────────┐
│  Execution 層（Broker 抽象化）                               │
│  - BacktestBroker（シミュレーション） / PaperBroker / LiveBroker │
└───────────────┬────────────────────────────────────────────┘
                │
┌───────────────▼────────────────────────────────────────────┐
│  Data 層（DataSource 抽象化）                                │
│  - JQuantsSource / YFinanceSource / CSVSource               │
└────────────────────────────────────────────────────────────┘
```

### 主要インターフェース（実装時の指針）

- `DataSource` … `get_prices(symbols, start, end) -> DataFrame` 等。実装を差し替え可能に。
- `FeatureBuilder` … 価格・財務データ → 特徴量。ML/ルール共通の入力を生成。
- `Strategy` … `generate_signals(features) -> signals`。状態を持たず純粋関数的に。
- `RiskManager` / `Portfolio` … signals → orders（サイジング・損切り・DD 制御）。
- `Broker` … `BacktestBroker` / `PaperBroker` / `LiveBroker` を同一 IF で。
- `BacktestEngine` … 上記を時系列ループで駆動。**ルックアヘッドバイアス厳禁**。
- `Metrics` … リターン、シャープ、最大DD、勝率、約定回数、コスト後損益などを算出。

### 想定ディレクトリ構成（着手時の目安）

```
.
├── CLAUDE.md
├── README.md
├── pyproject.toml            # 依存・ツール設定（uv / poetry いずれか）
├── config/
│   └── default.yaml          # ユニバース・リスクパラメータ・期間など
├── src/jtrade/
│   ├── data/                 # DataSource 実装（jquants, yfinance, csv）
│   ├── features/             # 特徴量生成
│   ├── strategies/           # 戦略（baseline ルールベース / ml）
│   ├── risk/                 # RiskManager, Portfolio, サイジング
│   ├── execution/            # Broker 抽象 + Backtest/Paper/Live
│   ├── backtest/             # BacktestEngine, Metrics
│   └── cli.py                # エントリポイント
├── notebooks/                # 探索・分析用
└── tests/                    # pytest
```

---

## 5. 開発上のルール（Claude への指示）

- **安全第一。** Phase 1 では実発注コードを書かない。`LiveBroker` はスタブに留める。
- **ルックアヘッドバイアスを作らない。** 未来のデータで過去の意思決定をしないこと。
  特徴量・シグナル生成では、その時点で入手可能なデータのみ使用する。
- **リスク管理は戦略から独立。** 戦略のバグがリスク層を貫通しない設計にする。
- **取引コストは常に織り込む。** コストゼロのバックテストは行わない。
- **秘密情報をコミットしない。** API キー・口座情報は `.env`（gitignore 済み）に置く。
- **再現性。** 乱数シードを固定し、バックテスト結果を再現可能にする。
- **テストを書く。** 特にリスク管理・約定シミュレーション・指標計算は単体テスト必須。
- 段階的に進め、各フェーズ完了時に本書の進捗表を更新する。

### 投資に関する注意（必読）

本システムは投資助言ではなく、損失リスクを伴う。バックテストの良好な結果は将来の
利益を保証しない。本番運用前に十分な検証・少額での試験運用を行うこと。

---

## 6. 開発コマンド（実装が進んだら追記）

> セットアップ・テスト・バックテスト実行コマンドは、コードを追加した時点でここに記載する。
> 例:
> - 環境構築: `uv sync` もしくは `pip install -e .`
> - テスト: `pytest`
> - バックテスト実行: `python -m jtrade.cli backtest --config config/default.yaml`

---

## 7. オープンな論点（次に決めること）

- [ ] J-Quants API の利用プラン（無料 / 有料）と取得データ範囲の確定
- [ ] ベースライン戦略（例: 移動平均クロス）の具体ルール
- [ ] ML を使う場合のターゲット定義（翌日リターン符号 / N日後リターン 等）と評価方法
- [ ] バックテスト対象期間・リバランス頻度（日次 / 週次）
- [ ] リスクパラメータの初期値（損切り%、1銘柄上限%、最大DD%、手数料・スリッページ前提）
- [ ] Phase 3 で使う証券 API の最終決定（kabu ステーション API 等）

# CLAUDE.md — 日本株・米国株 自動売買システム（マルチマーケット対応）

このファイルは Claude Code がこのリポジトリで作業する際のガイドであり、
プロジェクトの要件定義書を兼ねます。設計判断や前提が変わったら本書を更新してください。

> 📘 **株取引がはじめての方へ：** まず [`docs/初心者ガイド.md`](docs/初心者ガイド.md) を
> 読んでください。本書の内容を、専門用語なしでやさしく解説しています。

---

## 1. プロジェクト概要

**日本株と米国株の両方**を対象とした**自動売買システム**を構築する。
機械学習を視野に入れた戦略を、**バックテスト → ペーパートレード → 本番運用**の
順で段階的に実用化していく。安全性を最優先とし、いきなり実弾を投入しない。

**マルチマーケット方針：** データソース・Broker・市場カレンダー・通貨を抽象化し、
日本株（JPX）と米国株（US）を同一フレームワークで扱えるよう設計する。
1万円スタートでは、市場ごとに最適な運用方式が異なる（下記 2.3）。
- 日本株 … 単元未満株で**半自動**（シグナル自動・発注手動）。為替なし・日中取引。
- 米国株 … IBKR 等で**全自動も可能**（API＋端株対応）。為替あり・夜間取引・税務やや複雑。

### 開発フェーズ（段階的ゴール）

| フェーズ | 内容 | 実弾リスク | 状態 |
|---|---|---|---|
| **Phase 1** | バックテスト基盤（データ取得・特徴量・戦略・約定シミュレーション・評価） | なし | **基盤実装済み（合成データで動作）／磨き込み中** |
| Phase 2 | ペーパートレード（リアルタイムデータで仮想発注、挙動検証） | なし | 未着手 |
| Phase 3 | 本番運用（**約1万円から**・市場別の運用方式・リスク管理を厳重に） | あり | 未着手 |

**現時点のスコープは Phase 1（バックテスト基盤）。** Phase 2/3 を見据えて
インターフェースを抽象化しておくが、実発注コードは Phase 1 では書かない。
資金額（1万円）が効くのは Phase 3 のみで、Phase 1/2 は資金額に依存せず開発・検証する。

---

## 2. 確定した要件（ヒアリング結果）

| 項目 | 決定 | 補足 |
|---|---|---|
| 言語 / 実行環境 | **Python** | データ分析・ML・バックテスト資産が豊富 |
| 最初のゴール | **バックテスト基盤から** | 実弾リスクなしで戦略検証 |
| 戦略 | **機械学習を視野（未確定）** | 戦略は差し替え可能な設計に。まずルールベースのベースラインも用意 |
| 対象市場 | **日本株＋米国株（両対応で設計）** | DataSource/Broker/市場カレンダー/通貨を抽象化（下記 2.3） |
| 証券会社 / 発注API | **市場別に未定（相談）** | Broker 層を抽象化。日本=単元未満株(手動)、米国=IBKR 等(API) を想定 |
| 株価データソース | **未定（推奨を採用）** | 日本→ **J-Quants**、米国→ **別ソース**（後述）。両方を抽象化 |
| 対象ユニバース | **おまかせ** | 日本→**東証プライム中心**、米国→**主要・手頃な銘柄**から開始（後述） |
| リスク管理 | 損切り/利確・ポジションサイズ・最大DD制限・取引コスト/スリッページ | **全て必須要件**（米国は為替コストも含める） |
| 初期資金 | **約1万円から** | 少額スタート。Phase 3 の運用方式・コスト前提を強く規定（下記 2.2 / 2.3） |
| 本番運用方式 | **市場別**：日本=半自動 / 米国=全自動も可 | 日本は単元未満株で手動発注、米国は API で自動発注が可能 |

### 2.1 推奨デフォルト（未定項目に対する提案）

未定だった項目は、以下を初期デフォルトとして進める。変更したい場合は本書を更新。

- **データソース（日本株）: J-Quants API**
  JPX（日本取引所グループ）公式のヒストリカルデータ。日足・財務・信用取引・
  決算発表予定などが取得でき、日本株 ML に最も適する。無料プランあり。
  まずは無料プランで取得できる範囲（過去2年程度の日足）から始める。
- **データソース（米国株）: yfinance を起点に、必要に応じて専用ソースへ**
  米国株は `yfinance` で日足を手軽に取得できる。本格化したら IBKR のヒストリカルデータや
  Alpha Vantage / Polygon 等の専用ソースに差し替え可能にする（DataSource 抽象で吸収）。
  `yfinance` はプロトタイピング全般の補助としても使う（精度・網羅性は劣る点に注意）。
- **ユニバース: 市場ごとに定義**
  - 日本株 … 東証プライム上場の流動性上位（例: 売買代金上位 ~500 銘柄）。
  - 米国株 … 主要かつ手頃な価格帯の銘柄（1万円で1株以上 or 端株で買える範囲）。
  流動性フィルタで ML のノイズとスリッページを抑える。銘柄リストは市場別に設定ファイルで管理。
- **発注 Broker（Phase 3 想定）: 市場別に最適なものを選ぶ**
  - 日本株 … 単元未満株サービスで**半自動**（API 自動発注は不可。詳細 2.2）。
  - 米国株 … **IBKR（Interactive Brokers）** が第一候補。端株対応 ＋ API で**全自動**が可能（詳細 2.3）。
  Broker 抽象化により、どちらも同一インターフェースで扱う。

### 2.2 少額（1万円）スタートの制約と運用方式 ★重要

初期資金が約1万円であることが、本番運用（Phase 3）の方式を強く規定する。

**日本株は原則 100 株（1単元）単位**で取引するため、株価1,000円の銘柄でも
最低10万円が必要。1万円では単元株はほぼ買えない。そこで**単元未満株
（1株から購入可能なサービス）**を使う。

| 証券会社 | 単元未満株サービス |
|---|---|
| SBI証券 | S株 |
| 楽天証券 | かぶミニ |
| マネックス証券 | ワン株 |
| auカブコム証券 | プチ株 |

**重要な制約：単元未満株は自動発注 API にほぼ対応していない**
（アプリ・Web からの手動発注が中心）。一方、発注 API（kabu ステーション API 等）は
100 株単位が前提。**「1万円で買える」と「API で自動発注できる」は両立しない。**

#### 採用方式：案A＝単元未満株で「半自動」運用

- システムが**売買シグナルを自動生成**する（どの銘柄を何株、いつ売買すべきか）。
- **発注はユーザーが単元未満株サービスで手動実行**する（全自動ではない）。
- 1万円でも複数銘柄に分散でき、リスクを限定しながら少額で検証・習熟できる。
- 将来、資金が増えてフル自動（API・100株単位）へ移行する場合に備え、Execution 層は
  `LiveBroker`（API 自動）も差し替え可能な抽象として設計しておく（Phase 1 では未実装）。

#### 少額ゆえに守ること

- **取引コスト・スプレッドを保守的に見積もる。** 1万円規模では手数料・スプレッドが
  リターンを％で大きく圧迫する。バックテストには必ず保守的なコストを織り込む。
- **最低発注単位は1株。** サイジングは「1株刻み」を前提に丸める（端株購入できない）。
- **過剰な売買回数を避ける。** 回転が増えるほどコスト負けしやすい。
- バックテスト／ペーパー（Phase 1/2）は資金額に依存しないため、金額に関係なく
  フルに開発・検証する。1万円制約が効くのは Phase 3 の発注方式とコスト前提のみ。

### 2.3 米国株（海外株）対応と市場ごとの違い ★重要

1万円スタートでは、**米国株のほうが全自動に向く**。理由と注意点を整理する。

- **米国株には「100株単位」のルールがない。** 1株から買えるため、手頃な価格帯の銘柄なら
  1万円で複数銘柄に分散できる。さらに証券会社によっては**金額指定（端株／フラクショナル株）**で
  「1,000円分だけ」のような買い方も可能（例: PayPay証券）。
- **★ IBKR なら 1万円でも「全自動」が可能。** Interactive Brokers は
  **端株（フラクショナル株）対応 ＋ 発注 API** を両方備える。日本の単元未満株では両立しなかった
  「少額で買える」と「API で自動発注」を米国株では両立できる。

#### 日本株 vs 米国株（1万円スタートの比較）

| 観点 | 日本株 | 米国株 |
|---|---|---|
| 1万円で買えるか | 単元未満株なら ○ | ○（1株 or 金額指定の端株） |
| **全自動（API発注）** | ✕（半自動・発注は手動） | **○（IBKR 等で API 自動）** |
| 為替リスク・コスト | なし | あり（円⇄ドル、為替手数料） |
| 取引時間（日本時間） | 日中（9:00–15:00 頃） | 夜間（22:30–翌5:00 頃） |
| 税務 | シンプル | やや複雑（米国源泉徴収・外国税額控除等） |
| データソース | J-Quants | yfinance / IBKR / 専用API |

#### 設計上の含意（両対応のために必要なこと）

- **市場カレンダー（MarketCalendar）の抽象化。** JPX と US で営業日・祝日・取引時間が異なる。
  バックテストの時系列ループと、本番のスケジューリングは市場ごとのカレンダーに従う。
- **通貨（Currency）の扱い。** 米国株はドル建て。バックテストは「現地通貨建て」と
  「円換算」を扱えるようにし、**為替レートと為替手数料**をコストモデルに含める。
- **コストモデルは市場別。** 手数料体系・スプレッド・為替コストを市場ごとに設定可能にする。
- **運用方式は市場別に分岐。** 日本=半自動（手動発注）、米国=全自動（API）。Execution 層で吸収。

#### 米国株ゆえの注意

- 為替の影響で、現地通貨で勝っていても円換算で負ける（逆も）ことがある。評価は両建てで見る。
- 全自動は便利だが、夜間に無人で発注が走る。**リスク管理（最大DD・損切り・上限）を特に厳重に。**
- 税務・確定申告がやや複雑になる点を、本番前に確認する。

---

## 3. 必須リスク管理要件

バックテスト・本番いずれにおいても、以下を**戦略から独立した共通モジュール**として実装する。
戦略ロジックがバグっても、リスク層が最終防衛線として機能すること。

1. **損切り / 利確（ストップ）**
   ポジションごとに stop-loss / take-profit を設定。トレーリングストップも検討。
2. **ポジションサイズ管理**
   1 銘柄あたりの最大エクスポージャ、ポートフォリオ全体の最大エクスポージャを制限。
   固定比率・ボラティリティ（ATR）ベースのサイジングをサポート。
3. **最大ドローダウン制限**
   一定のドローダウンに達したら新規エントリーを停止するサーキットブレーカー。
4. **取引コスト・スリッページのモデル化**
   手数料（証券会社の体系）とスリッページをバックテストに必ず織り込む。
   楽観的なバックテスト結果を防ぐため、保守的なコスト前提を既定とする。
   **米国株では為替手数料・為替変動も「コスト／損益」に含める。**

---

## 4. アーキテクチャ方針

戦略・データソース・発注先を**いつでも差し替えられる**よう、各層をインターフェースで分離する。

```
┌────────────────────────────────────────────────────────────┐
│  Strategy 層（差し替え可能：ルールベース / ML）              │
│  - シグナル生成のみ担当。発注やリスク判定は持たない           │
└───────────────┬────────────────────────────────────────────┘
                │ signals
┌───────────────▼────────────────────────────────────────────┐
│  Risk / Portfolio 層（共通・必須）                           │
│  - ポジションサイズ、損切り利確、最大DD、コスト適用           │
└───────────────┬────────────────────────────────────────────┘
                │ orders
┌───────────────▼────────────────────────────────────────────┐
│  Execution 層（Broker 抽象化／市場別）                       │
│  - BacktestBroker / PaperBroker                              │
│  - 日本: ManualFractionalBroker（半自動・発注指示の出力）     │
│  - 米国: IBKRBroker（API 全自動）                            │
└───────────────┬────────────────────────────────────────────┘
                │
┌───────────────▼────────────────────────────────────────────┐
│  Data 層（DataSource 抽象化）＋ 市場カレンダー／通貨          │
│  - 日本: JQuantsSource   米国: YFinanceSource / 専用API      │
│  - MarketCalendar（JPX/US）, Currency（JPY/USD・為替）       │
└────────────────────────────────────────────────────────────┘
```

### 主要インターフェース（実装時の指針）

- `DataSource` … `get_prices(symbols, start, end) -> DataFrame` 等。市場別実装を差し替え可能に。
- `MarketCalendar` … 市場（JPX/US）ごとの営業日・取引時間。ループとスケジューリングの基準。
- `Currency` / FX … 現地通貨と円換算、為替レート・為替手数料を扱う。
- `FeatureBuilder` … 価格・財務データ → 特徴量。ML/ルール共通の入力を生成。
- `Strategy` … `generate_signals(features) -> signals`。状態を持たず純粋関数的に。市場非依存。
- `RiskManager` / `Portfolio` … signals → orders（サイジング・損切り・DD 制御）。市場別コスト適用。
- `Broker` … `BacktestBroker` / `PaperBroker` / `ManualFractionalBroker`(日本・半自動) /
  `IBKRBroker`(米国・全自動) を同一 IF で。
- `BacktestEngine` … 上記を時系列ループで駆動。**ルックアヘッドバイアス厳禁**。
- `Metrics` … リターン、シャープ、最大DD、勝率、約定回数、コスト後損益などを算出（円・現地通貨両建て）。

### 想定ディレクトリ構成（着手時の目安）

```
.
├── CLAUDE.md
├── README.md
├── pyproject.toml            # 依存・ツール設定（uv / poetry いずれか）
├── config/
│   ├── jp.yaml               # 日本株: ユニバース・リスク・コスト・期間など
│   └── us.yaml               # 米国株: ユニバース・リスク・為替コスト・期間など
├── src/autotrade/            # 市場非依存のパッケージ名（日本株/米国株 両対応）
│   ├── data/                 # DataSource 実装（jquants, yfinance, 専用API, csv）
│   ├── markets/              # MarketCalendar（JPX/US）, Currency / FX
│   ├── features/             # 特徴量生成
│   ├── strategies/           # 戦略（baseline ルールベース / ml）市場非依存
│   ├── risk/                 # RiskManager, Portfolio, サイジング（市場別コスト）
│   ├── execution/            # Broker 抽象 + Backtest/Paper + 日本(半自動)/米国(IBKR)
│   ├── backtest/             # BacktestEngine, Metrics
│   └── cli.py                # エントリポイント
├── notebooks/                # 探索・分析用
└── tests/                    # pytest
```

---

## 5. 開発上のルール（Claude への指示）

- **株初心者にもわかる言葉で説明する。★最優先**
  ユーザーは株取引の初心者である。回答・説明・コミットメッセージ・コメントでは、
  専門用語をそのまま使わず、初心者にもわかるやさしい言葉で説明すること。
  - 専門用語（例: シャープレシオ、最大ドローダウン、プロフィットファクター、ボラティリティ、
    エクスポージャ、約定、ルックアヘッドバイアス 等）を使うときは、**必ず一言で意味を添える**。
    例:「シャープレシオ（リスクに対してどれだけ効率よく儲けたかの指標。高いほど良い）」。
  - 結論・数字の意味を先に日常語で述べ、専門用語は補足として括弧で添える。
  - 迷ったら [`docs/初心者ガイド.md`](docs/初心者ガイド.md) のトーンに合わせる。
  - 略語・英語の指標名は初出時に日本語の説明を付ける。
- **安全第一。** Phase 1 では実発注コードを書かない。`LiveBroker` はスタブに留める。
- **ルックアヘッドバイアスを作らない。** 未来のデータで過去の意思決定をしないこと。
  特徴量・シグナル生成では、その時点で入手可能なデータのみ使用する。
- **リスク管理は戦略から独立。** 戦略のバグがリスク層を貫通しない設計にする。
- **取引コストは常に織り込む。** コストゼロのバックテストは行わない。
- **秘密情報をコミットしない。** API キー・口座情報は `.env`（gitignore 済み）に置く。
- **再現性。** 乱数シードを固定し、バックテスト結果を再現可能にする。
- **テストを書く。** 特にリスク管理・約定シミュレーション・指標計算は単体テスト必須。
- 段階的に進め、各フェーズ完了時に本書の進捗表を更新する。

### 投資に関する注意（必読）

本システムは投資助言ではなく、損失リスクを伴う。バックテストの良好な結果は将来の
利益を保証しない。本番運用前に十分な検証・少額での試験運用を行うこと。

---

## 6. 開発コマンド

- 環境構築（本体）: `pip install -e .`
- 環境構築（テスト/実データ）: `pip install -e ".[dev]"` / `pip install -e ".[data]"`
- テスト: `pytest`
- バックテスト（日本株・既定は合成データでネット不要）:
  `python -m autotrade.cli backtest --config config/jp.yaml`
- バックテスト（米国株・為替手数料込み）:
  `python -m autotrade.cli backtest --config config/us.yaml`
- 資産曲線を保存: `... --save-equity output/equity.csv`

> 実データは設定の `data.source` を `yfinance` にし、`universe` を実ティッカー
> （日本株 `7203.T` / 米国株 `AAPL` 等）へ。J-Quants・IBKR は DataSource/Broker 抽象で後付け。

### Phase 1 の実装状況（2026-06 時点）

データ（合成/CSV/yfinance）・特徴量（SMA/RSI/ATR/長期トレンドsma_trend）・
戦略（移動平均クロス / トレンドフィルター / ML=ロジスティック回帰 / 横断モメンタム
xs_momentum、ML はランキングモードも）・リスク（損切り・**利確(任意/オフ可)**・
**トレーリングストップ**・サイジング・最大DDブレーカー）・約定（コストモデル付き
BacktestBroker）・評価（Metrics）・**バイ&ホールド比較**・**ウォークフォワード検証**・
CLI・テスト（35件）まで実装済みで、実データ（yfinance）でエンドツーエンドに動作する。

**検証コマンド:**
- バックテスト＋持ち続け比較: `python -m autotrade.cli backtest --config config/jp_real.yaml`
- 戦略の上書き: `... --strategy trend_filter`（`ml_logreg` / `xs_momentum`）
- ウォークフォワード: `python -m autotrade.cli walkforward --config config/us_real.yaml --start 2015-01-01`
- 横断ランキング（30銘柄ユニバース）: `... backtest --config config/us_xs.yaml`

### ★重要な検証結果（2026-06）— まだ「持ち続け」に勝てていない

実データ（日米の大型優良株, 2015–2023）での厳密な検証（ウォークフォワード・コスト込み）で、
**現状の全戦略（移動平均クロス / トレンドフィルター / ML / 横断モメンタム）が、単純な
バイ&ホールド（等金額で買って持ち続ける）にリターン・シャープ・最大DDのすべてで敗北**している。
- 例（米国, 7年OOS）: WF戦略 +28.6% vs 持ち続け +250.4%。横断モメンタムも +93% vs +585%。
- 教訓1: daily足の「売買タイミング当て」「上位選別」は上昇相場の大型株では価値を破壊しがち。
  **新戦略は必ずバイ&ホールドと比較**し、勝てて初めて意味がある。手チューニングで一期間の
  数字を追うのはオーバーフィッティング（ウォークフォワードで判断する）。
- 教訓2: **利確(take_profit)はモメンタム系と相性が悪い**（大化け株を途中で売る）。
  利確オフで米国モメンタムは -30%→+93% に改善。`take_profit_pct: null` + `trailing_stop_pct` が定石。
- 教訓3: 日本株の少額(1万円)×多銘柄テストは整数株丸めで破綻しやすい（高価格株を買えない）。
- 次の方向性: **ロングショート**（弱い銘柄を空売り＝持ち続けに構造的に勝てる可能性）/ 別ユニバース /
  ファンダメンタル等の特徴量 / 「絶対方向」でなく「相対強弱」を予測するターゲット設計。
- **現在着手中: ロングショート戦略（横断モメンタムの上位ロング・下位ショート）。**

---

## 7. オープンな論点（次に決めること）

- [x] **最初に着手する市場** → 日米両対応で並行検証中（yfinance 実データ）
- [ ] J-Quants API の利用プラン（無料 / 有料）と取得データ範囲の確定（日本株）
- [x] 米国株データソース → **yfinance で開始**（後に IBKR / 専用API へ差し替え予定）
- [x] ベースライン戦略 → 移動平均クロス / トレンドフィルター / 横断モメンタムを実装済み
- [x] ML のターゲット定義 → **N日後（既定5日）リターンの符号**、拡張窓でリーク防止。評価はWF
- [x] バックテスト対象期間・リバランス頻度 → 日次、検証は 2015–2023 を使用
- [~] リスクパラメータ初期値 → config に既定値あり（損切り8–20%/1銘柄10–20%/最大DD20–35%）。要調整
- [ ] **（最優先・未解決）持ち続け（バイ&ホールド）に勝てる戦略の発見** ← ロングショートで挑戦中
- [ ] 日本株: 単元未満株サービスの選定（S株 / かぶミニ / ワン株 / プチ株）と手数料前提
- [ ] 米国株: IBKR 口座・API 利用の可否、為替手数料・税務の確認
- [ ] 半自動運用（日本株）のシグナル受け取り方法（CSV出力 / 通知 / 簡易ダッシュボード等）
